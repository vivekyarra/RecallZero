# RecallZero — judge demo video (2:40 target)

This is a real one-take browser recording of `https://recallzero.vercel.app`, with actual pointer movement, hover, clicks, server responses, and state transitions. ElevenLabs voice: **Chris — Charming, Down-to-Earth**, Multilingual v2. The nine voice files are kept in `out/demo-video/` locally. Each screen beat is held for the measured duration of its matching voice file plus 0.55 seconds; no screenshots or fabricated UI states appear in the video.

| Beat | Screen action | Exact narration |
| --- | --- | --- |
| 1 — The gap | Show the hero, hover “0 unresolved” and the import control. | A recall can be public, and the dangerous product can still be in your home. That gap is the problem. RecallZero turns an exact safety notice into a governed path to remedy, with one clear human handoff. |
| 2 — Ownership | Click **Import demo receipt**; hover the model and synthetic receipt badge. | This synthetic receipt belongs to a Wantefully X R eighty-eight oh one hair dryer. One click turns its model, retailer, and purchase date into an Asset Passport. The purchase is demo data; the recall source is official. |
| 3 — Exactness | Click **Run live recall check**; hover the actual source badge, matching predicates, and official CPSC link. | Now I run the on-demand C P S C check. The badge shows whether the live A P I answered or an official snapshot was used. Four deterministic checks establish exact identity. A similar product is only a possible match; it cannot open a remedy contract. |
| 4 — Contract | Click **Let RecallZero handle it**; hover the physical gate and contract identifier. | Here is the key moment. The exact match becomes a Remedy Contract. It fixes the official instruction, the sandbox destination, and the completion conditions. The public journey mirrors a separate Strands runtime that accepts only this verified contract and exposes five narrow tools. |
| 5 — Human handoff | Hold the official physical instruction; click **Use prepared synthetic proof**. | The official remedy requires the unplugged dryer to be disabled and photographed. Software cannot do that. RecallZero stops at one precise human task. For this video I choose prepared synthetic evidence. No real product is damaged. |
| 6 — Submission | Click **Submit to manufacturer sandbox**; hover the returned receipt and open state. | The request goes only to a controlled manufacturer sandbox. It returns a stable receipt. There is no real manufacturer contact, and this is not a real refund. A submitted request still leaves the recall open. |
| 7 — Outcome | Click **Fast-forward sandbox outcome**, then **Verify contract completion**; show zero unresolved. | I fast-forward a simulated provider approval, then run the completion check. Evidence alone cannot close this case. A provider token alone cannot close it either. Both must belong to this contract. Only then does the unresolved count return to zero. |
| 8 — Architecture | Click **How it works**; hover the source, deterministic gate, tool boundary, and human responsibility. | Under the hood, C P S C supplies the safety fact. Deterministic code proves the exact match and controls state transitions. Strands has five allowlisted tools for routine remedy work. The browser demo is a mirror; the separate runtime and tests record actual local Strands S D K tool calls. |
| 9 — Proof | Execute **Simulate Attack**, **Test Bypass**, and **Test Forgery**; open the recorded SDK trace. | These buttons execute real adversarial checks. An injected product name cannot become an exact match. Missing evidence blocks submission. A swapped asset fails contract binding. The trace records local Strands S D K calls, not a Bedrock model or AgentCore deployment. The promise is less recall busywork, with safety truth still in code. |

The public source mode may show `LIVE_CPSC` or the clearly labeled official snapshot fallback. Record the badge honestly. The sandbox provider approval and evidence are synthetic. The local Strands trace is a real SDK tool-boundary record, not an LLM-driven Bedrock invocation.
